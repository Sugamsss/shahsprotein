// The admin as a home-screen app (src/admin/homeScreenApp.ts). Keep the name in
// step with public/admin.webmanifest, which can't import it.
export const adminAppCopy = {
  name: "Shah's Orders",
} as const;
