import React from 'react';
import { adminCopy as copy } from '../../data/adminCopy';
import { ComingNext } from './ComingNext';

const ProductsPage: React.FC = () => <ComingNext title={copy.pages.products} />;

export default ProductsPage;
